{
  "name": "@fonteditor/catalog",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Querying a font's glyphs: sets, filters, search, ordering. Pure; no DOM.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fonteditor/font-model": "workspace:*"
  }
}
